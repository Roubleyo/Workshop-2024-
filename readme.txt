Pour une première utilisation, bien install les requirements, puis lancer le script `app.py` avec la commande suivante :

```
streamlit run app.py
```

La partie detoxify va devoir télécharger le modèle, cela peut prendre un peu de temps.
le téléchargement du modèle est fait une seule fois, les prochaines utilisations seront plus rapides.
